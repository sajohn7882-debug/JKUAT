{
  "name": "jkuat-wayfinder",
  "version": "1.0.0",
  "description": "JKUAT Wayfinder - Campus Navigation and Student Utility",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "node server.js",
    "lint": "node --check server.js && node --check api/index.js && node --check firebase.js",
    "build": "node -e \"console.log('Build complete')\"",
    "start": "node server.js"
  },
  "dependencies": {
    "@google/genai": "^2.21.0",
    "express": "^4.21.2",
    "firebase": "^12.18.0"
  },
  "devDependencies": {
    "@firebase/eslint-plugin-security-rules": "^0.0.2"
  }
}
